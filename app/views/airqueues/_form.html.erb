<%= form_for(@airqueue) do |f| %>
  <% if @airqueue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@airqueue.errors.count, "error") %> prohibited this airqueue from being saved:</h2>

      <ul>
      <% @airqueue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
